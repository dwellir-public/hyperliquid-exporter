# logger

Structured, colorized logging with component-aware categorization and level filtering.

Components (CORE, EVM, REPLICA, CONSENSUS, etc.) are automatically color-coded in output. Four log levels are supported: DEBUG, INFO, WARNING, ERROR. Timestamps use microsecond precision.
